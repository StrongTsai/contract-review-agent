[project]
name = "contract-review-agent"
version = "0.1.0"
description = "合同审核数字员工:LangGraph + DeepSeek 多节点审核 agent"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langchain-openai",
    "langchain-core",
    "pydantic",
    "python-dotenv",
    "sentence-transformers>=6.0.1",
    "langchain>=1.4.0",
    "rapidocr>=3.9.2",
    "onnxruntime>=1.30.0",
]
